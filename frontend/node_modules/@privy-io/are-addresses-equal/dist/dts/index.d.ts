/**
 * Determine if two wallet addresses are equivalent.
 *
 * @param add1 first address to compare
 * @param add2 second address to compare
 * @returns true if the wallet addresses are the same
 */
declare const areAddressesEqual: (add1: string, add2: string) => boolean;

declare const VERSION = "__VERSION__";

export { VERSION, areAddressesEqual };
